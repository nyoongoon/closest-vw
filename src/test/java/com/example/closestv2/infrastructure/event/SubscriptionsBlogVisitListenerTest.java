package com.example.closestv2.infrastructure.event;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class SubscriptionsBlogVisitListenerTest {
    @Test
    @DisplayName("SubscriptionsBlogVisit 이벤트 발생 시 이벤트를 수신한다. ")
    void onSubscriptionsBlogVisitListen(){
        //given
        //when
        //then
        throw new IllegalArgumentException();
    }

    @Test
    @DisplayName("SubscriptionsBlogVisit 이벤트를 수신해서 @@을 처리한다.")
    void something(){
        //given
        //when
        //then
        throw new IllegalArgumentException();
    }
}