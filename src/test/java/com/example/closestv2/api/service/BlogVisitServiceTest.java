package com.example.closestv2.api.service;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;


class BlogVisitServiceTest {

    @Test
    @DisplayName("")
    void visitBlog(){
        //given
        //when
        //then
        throw new IllegalArgumentException("블로그를 방문하면 visitCount가 1증가한다.");
    }

    @Test
    @DisplayName("")
    void visitPost(){
        //given
        //when
        //then
        throw new IllegalArgumentException("포스트를 방문하면 블로그와 포스트의 visitCount가 1 증가한다.");
    }
}